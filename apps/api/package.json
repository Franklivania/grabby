{
  "name": "@grabby/api",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "bunx tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "lint": "bunx eslint src tests --ext .ts",
    "test": "bun test"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@grabby/core": "workspace:*",
    "fastify": "^5.2.2",
    "playwright": "^1.52.0"
  }
}
